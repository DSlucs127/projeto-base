{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the primary application window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
